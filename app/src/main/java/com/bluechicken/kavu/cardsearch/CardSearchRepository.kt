package com.bluechicken.kavu.cardsearch

class CardSearchRepository {

    /*
    fun getSearchedCards()

    companion object {
        @Volatile private var instance: CardSearchRepository? = null

        fun getInstance() =
            instance
                ?: synchronized(this) {
                instance
                    ?: CardSearchRepository()
                        .also { instance = it}
            }
    }
     */
}